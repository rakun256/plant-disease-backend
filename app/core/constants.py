# ML Constants
SUPPORTED_CLASSES = ["healthy", "rust", "scab"]
WARNING_MESSAGE = "The model currently only supports apple leaf classes from the specified academic dataset."
LOW_CONFIDENCE_WARNING_MESSAGE = "The model confidence is low, and the image may be outside the supported apple leaf classes. Please retake the image under better lighting or consult an agricultural expert."

# Auth Constants
ALGORITHM = "HS256"
